{
  "fixRow554": "| 554 | TASKS #128（用户指派「做这个」）：字卡库 44.59MB 级膨胀的结构性收敛——聊天图自 #142 走媒体池去重，字卡图一直整份内联（#160 实测双作用域 62.8MB），同一张贴图出现在几个组/几个作用域就存几份；#377/#455 内存令牌化只省内存（原始键一字节不动） | 双入口落地（`src/js/chatcard.js`）：①**上传口**——表情包/图片 ≥`CC_CC_TOK_MIN`(4096) 先 `mochiMediaTokenize` 写池、库键只存 `@@m:` 令牌（池失败回退内联，上传永不因池而丢）；②**存量迁移** `window.mochiCcPersistTokenize(prog)`——正则字符串级定位（绝不 JSON.parse 整库＝#455 纪律）、逐张写池拿令牌（跨卡/跨组/跨作用域同图同哈希只存一份）、**池先令牌后**（`mochiMediaFlush` 强制落盘后才写库键，崩溃窗口最多「池多一条孤儿」）、不变小不写＋失败整库放弃双保险丝、`pubInvalidate` 同拍失效。消费方此前已就绪：渲染（#142/#377 观察器）、GC 引用面含 cc-groups（#506）、字卡导出自包含还原（#506 ccExportExpandTokens）、完整备份自带池键（#275）、字卡自检令牌感知（#532）；语音 v1 不动。入口：设置→查看存储→「字卡图去重入库」 | 哨兵 4 条（build.mjs #554a~d）。验证：`node --check` 过；新增 `tools/verify-cc-media-persist-token.mjs` **17/17**（B 组无头真实产物：种子双作用域库→迁移→库键大图全变令牌/小图语音文字卡原样、池字节与原图逐字节一致且唯一、跨组跨分类跨作用域同令牌、TokenMissing=false、GC 不误删、幂等 written=0、报告口径；B8 同时覆盖 #560）；`--check-sentinels` **1330/1330 哑 0**；回归 verify-media-pool 10/10、verify-perf-heal 18/18。**RED 基线**：HEAD 无 mochiCcPersistTokenize（git show 可证）。【真机:待验证】设置→查看存储→字卡图去重入库→确认后看「各字卡库合计」明显变小；聊天里抽字卡表情/图片照常显示；导出数据仍是含完整图片的自包含包 | 与 #142（聊天图池去重）互补成完整闭环：同图「聊天+收藏+字卡库」全域只存一份；零机型分支 |",
  "fixRow560": "| 560 | 存量 bug（#554 插桩发现，非用户新报）：字卡库瘦身「整组删除」报成功、刷新后分组复活——删除从未真正落库 | 根因（纯逻辑零机型分支）：`storage-slim.js` libs 的 prefix 带尾冒号（`G='xy-home-v2:'`），`mochiCcSlimDeleteGroup` 写回 `xyStore(prefix).set(key)` 内部再拼 `':'+k`＝写出 `xy-home-v2::cc-groups-public` 双冒号垃圾键；读路径字符串直拼命中真实键，故扫描/确认全正常、唯独落库写错键。自 #170 上线起即存在 | `mochiCcSlimDeleteGroup` 剥尾冒号（`pStore=String(prefix).replace(/:$/,'')`）再进 xyStore + 同字节 idbSet durable 收尾 | `tools/verify-cc-media-persist-token.mjs` B8a/B8b（删除后真实键分组消失 + 不再产生 `xy-home-v2::` 垃圾键）；哨兵 build.mjs #560。【真机:待验证】瘦身删一个分组→重进不复活 | 修复随 #554 批次构建上车 |",
  "worklogBanner": "> **本次构建者：AI-B（本会话·#554/#560 收口，用户直接指派「做这个」）**；改 `src/js/chatcard.js`、`src/js/storage-slim.js`、`src/js/personalize.js`、`src/template.html`（storage-slim 等为 #170/#411 族 AI-B 域；chatcard.js 属 AI-A 域，**跨域改动，理由：TASKS #128 点名 chatcard.js，用户直接指派**）+ 共享 `build.mjs`（#554a~d、#560 哨兵）+ 新增 `tools/verify-cc-media-persist-token.mjs`（17 断言）。构建含树上全部在途稿；哨兵 1330/1330 哑 0。⚠️ 本会话期间再次发生并行整文件保存覆盖：chatcard/template/personalize 三处编辑被冲掉后已按磁盘最新版重套。**需对方处理**：①#556 会话——`mobile-adapt.js` 在途稿被打掉，构建报 #556a~c 三条哨兵 src 缺失（请重套你的键盘白带修复并重建）；②#547 会话——chatcard `ccTokenizeGiantMedia` 片段新增 `ccTokMemoRev` 引用致 `verify-cc-own-pool-slim` 抽取 eval 崩（ReferenceError，声明在抽取边界外）；③#533 脚本 owner——`verify-url-card-text-pool` A8/A9 的 HEAD RED 对照因 mail.js/calendar.js 已演进而失败（21/2，非回归）。",
  "worklogEntry": "### 2026-09-16 07:4x 【AI-B 域（用户直接指派 TASKS #128），跨域 chatcard.js；构建者＝本会话】#554 字卡媒体令牌化持久化（库键瘦身：同图全库只存一份）+ #560 瘦身删除垃圾键修复\n- [改动 **src/js/chatcard.js**（上传口 ≥4096 先写池存令牌 + `mochiCcPersistTokenize` 存量迁移：字符串级替换/池先令牌后/不变小不写/幂等/pubInvalidate 同拍）、**src/js/storage-slim.js**（#560 剥尾冒号修双冒号垃圾键 + durable 收尾）、**src/js/personalize.js** + **src/template.html**（查看存储「字卡图去重入库」入口）、**build.mjs**（#554a~d/#560 哨兵）、**tools/verify-cc-media-persist-token.mjs**（新增，17 断言）]（构建状态：已构建，含树上全部在途稿）。\n- 需求：用户指派 TASKS #128「做这个」。设计要点：池先令牌后（对齐 normalize「先 flush 池再存」契约，崩溃窗口只多孤儿绝不丢图）；绝不 JSON.parse 整库（#455 纪律）；语音 v1 不动（管理页预览未随批验证）。\n- 插桩发现并同批修复 #560：storage-slim 删除写 `xy-home-v2::` 双冒号垃圾键＝#170 删除从未落库（刷新复活）。\n- 验证：verify-cc-media-persist-token **17/17**（含 #560 回归断言）；verify-media-pool 10/10、verify-perf-heal 18/18；哨兵 1330/1330 哑 0。#455 套件崩与 url-card-pool 21/2 归属见横幅「需对方处理」，与本批无关（0 处涉及）。\n- 【真机:待验证】①查看存储→字卡图去重入库→各字卡库合计明显变小；②字卡表情/图片聊天抽发照常、导出数据仍自包含；③瘦身删组重进不复活（#560）。\n"
}
